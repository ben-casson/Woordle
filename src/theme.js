// export const setTheme = theme => document.documentElement.className = theme;

export const themeButton = document.getElementById("theme-button");
// themeButton.addEventListener('click', () => {
//     document.body.className ==='dark' ? document.body.className ='light' : document.body.className ='dark';
// });